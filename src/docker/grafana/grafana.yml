version: '3'
services:
  grafana:
    image: "grafana/grafana"
    environment:
      GF_INSTALL_PLUGINS: simpod-json-datasource,novatec-sdg-panel
    volumes:
      - grafana-storage:/var/lib/grafana
    ports:
      - "3000:3000"
volumes:
  grafana-storage: